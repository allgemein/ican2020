/*
   寝落ち検知アルゴリズム
 */

// bool isNeochi(vec3 head, vec3 wrist, float temp, float pulse) {	
// 	bool neochi = (millis()<5000?false:(head.z<threshold));
// 	//bool neochi = (-0.6<wrist.z);
// 	if(neochi)digitalWrite(13,HIGH);
// 	else digitalWrite(13,LOW);
// 	return neochi;
// }
